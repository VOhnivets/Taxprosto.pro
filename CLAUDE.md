# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

# taxprosto.pro

## Продукт
Вебзастосунок для самостійного заповнення річної податкової декларації про майновий стан і доходи (форма **F0100215**) громадянами України.

**Ключовий flow:**
1. Користувач завантажує довідку про доходи — форму **F1419104** «Відомості з ДРФО» (PDF з Електронного кабінету / Дії)
2. Застосунок парсить дані (податковий агент, нарахований дохід, утриманий ПДФО/ВЗ)
3. Формує XML за XSD-схемою ДПС (F0100215), windows-1251
4. Користувач імпортує XML в Електронний кабінет (cabinet.tax.gov.ua)

## Аудиторія
Фізичні особи, яким **потрібно** подати декларацію (не «усі зі зарплатою»). Типовий найманець лише з утриманим ПДФО/ВЗ декларацію не подає (п. 179.2 ПКУ).

Поза MVP, але в продуктовому беклозі: ФОП 3 група / загальна система, іноземні доходи, податкова знижка.

## Стадія
MVP: парсинг F1419104 → перевірка → генерація XML. Live: https://taxprosto.pro/ (GitHub Pages з `main` цього репозиторію).

Покриття:

- код **101** → 10.1 (ПДФО ≈18%) або 10.3 (Дія Сіті, ≈5%)
- код **102** → 10.2 (ЦПД ≈18%) або 10.3 (гіг Дія Сіті, ≈5%)
- код **129** → рядок 11.3 (пп. 165.1.2 ПКУ)

Самостійні зобов’язання (рядки 20+, додатки Ф1/Ф2), іноземні доходи, знижка — поза MVP.

**Домен**: https://taxprosto.pro/

---

## Технічний контекст

- **Схема XML**: форма **F0100215** (наказ МФУ №859, у ред. наказу №119 від 26.02.2025). Локально: `knowledge/xsd/F0100215.xsd` + `common_types.xsd`. Коди: C_DOC=F01, C_DOC_SUB=002, C_DOC_VER=15.
- **Джерело**: PDF F1419104. Парсер — pdf.js 4.10.38 (CDN, SRI; `isEvalSupported: false`).
- **Цільова система**: cabinet.tax.gov.ua → «Звітність» → «Імпорт XML»
- **Валідація**: клієнтська `validate()` (обов’язкові C_REG/C_RAJ/C_STI_ORIG/HCITY/HSTREET/HSTI, checksum РНОКПП) + `xmllint --schema knowledge/xsd/F0100215.xsd` у `node tests/run.mjs`
- **Архітектура**: 100% client-side, один `index.html`. Чиста логіка між маркерами `PURE_START` / `PURE_END`.

## Структура папок

- `index.html` — увесь застосунок
- `tests/run.mjs` — checksum, парсер, generateXML, xmllint
- `knowledge/xsd/` — офіційні XSD ДПС
- `docs/` — аудити
- `output/` — прототипи
- `.claude/skills/tax-consultant/` — skill податкового консультанта

---

## Мова
Спілкування — українська. Технічні терміни (XML, XSD, PDF, MVP, API, DOM, HTML, JS) — англійськими.

## Стиль
- Спочатку мінімально робочий варіант, потім розширення
- Не теоретизувати — давати код і приклади

## Обмеження
- Не відправляти дані на сервер без явного рішення про архітектуру (приватність)
- Не комітити персональні дані: `input/`, `knowledge/rules/`, згенеровані `*.xml` — gitignored
- Не обіцяти в UI покриття, якого немає в генераторі XML
